import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  content: {
    padding: 10,
    gap: 10,
  },
  column: {
    gap: 10,
  },
});

export default styles;
